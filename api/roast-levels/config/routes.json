{
  "routes": [
    {
      "method": "GET",
      "path": "/roast-levels",
      "handler": "roast-levels.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/roast-levels/count",
      "handler": "roast-levels.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/roast-levels/:id",
      "handler": "roast-levels.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/roast-levels",
      "handler": "roast-levels.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/roast-levels/:id",
      "handler": "roast-levels.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/roast-levels/:id",
      "handler": "roast-levels.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
